import React from "react";
import './CustomSpinner.css'

const CustomSpinner = () => {
  return (
    <div class="container">
      <div class="spinner"></div>
    </div>
  );
};

export default CustomSpinner;
